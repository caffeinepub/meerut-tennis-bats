# Specification

## Summary
**Goal:** Ensure all user-visible occurrences of the exact phrase "premium raza cricket bat" append the Raza name immediately after the phrase.

**Planned changes:**
- Locate every visible UI text instance that exactly matches "premium raza cricket bat" and update it to include the Raza name immediately after the phrase.
- Leave all other product copy unchanged.

**User-visible outcome:** Anywhere users see the exact text "premium raza cricket bat" in the UI, it now displays with the Raza name appended right after it.
